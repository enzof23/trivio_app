import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import {
  faShuffle,
  faDog,
  faVihara,
  faPalette,
  faChess,
  faBook,
  faStar,
  faBurst,
  faComputer,
  faFilm,
  faRobot,
  faLightbulb,
  faEarthEurope,
  faLandmark,
  faSquareRootVariable,
  faMusic,
  faMasksTheater,
  faDragon,
  faScaleUnbalancedFlip,
  faAtom,
  faFutbol,
  faTv,
  faTruckMonster,
  faGamepad,
  faAppleWhole,
  faArrowLeft,
} from "@fortawesome/free-solid-svg-icons";

const random = <FontAwesomeIcon icon={faShuffle} inverse />;
const animal = <FontAwesomeIcon icon={faDog} inverse />;
const anime_manga = <FontAwesomeIcon icon={faVihara} inverse />;
const art = <FontAwesomeIcon icon={faPalette} inverse />;
const board_games = <FontAwesomeIcon icon={faChess} inverse />;
const book = <FontAwesomeIcon icon={faBook} inverse />;
const celebrities = <FontAwesomeIcon icon={faStar} inverse />;
const comics = <FontAwesomeIcon icon={faBurst} inverse />;
const computer = <FontAwesomeIcon icon={faComputer} inverse />;
const film = <FontAwesomeIcon icon={faFilm} inverse />;
const gadget = <FontAwesomeIcon icon={faRobot} inverse />;
const general = <FontAwesomeIcon icon={faLightbulb} inverse />;
const geography = <FontAwesomeIcon icon={faEarthEurope} inverse />;
const history = <FontAwesomeIcon icon={faLandmark} inverse />;
const mathematics = <FontAwesomeIcon icon={faSquareRootVariable} inverse />;
const music = <FontAwesomeIcon icon={faMusic} inverse />;
const musical_theater = <FontAwesomeIcon icon={faMasksTheater} inverse />;
const mythology = <FontAwesomeIcon icon={faDragon} inverse />;
const politic = <FontAwesomeIcon icon={faScaleUnbalancedFlip} inverse />;
const science_nature = <FontAwesomeIcon icon={faAtom} inverse />;
const sport = <FontAwesomeIcon icon={faFutbol} inverse />;
const television = <FontAwesomeIcon icon={faTv} inverse />;
const vehicles = <FontAwesomeIcon icon={faTruckMonster} inverse />;
const video_games = <FontAwesomeIcon icon={faGamepad} inverse />;
const cartoon_animation = <FontAwesomeIcon icon={faAppleWhole} inverse />;
const go_back = <FontAwesomeIcon icon={faArrowLeft} />;

export const icons = {
  animal,
  anime_manga,
  art,
  board_games,
  book,
  cartoon_animation,
  celebrities,
  comics,
  computer,
  film,
  gadget,
  general,
  geography,
  history,
  mathematics,
  music,
  musical_theater,
  mythology,
  politic,
  random,
  science_nature,
  sport,
  television,
  vehicles,
  video_games,
  go_back,
};
